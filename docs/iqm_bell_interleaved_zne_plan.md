# IQM: five interleaved ZNE scales — execution plan

Approved scope: scales [1,1.5,2,3,5], STANDARD_DD_STRATEGY, no twirling or readout mitigation, same saved 50-draw schedule, 1000 shots per draw/scale, 250000 total. Work on codex/bell-optimized-benchmark; no commits or pushes.

- [x] Add scripts/iqm_bell_interleaved_zne.py and focused tests. Build four variants per block/scale, 250 shots each. Distribute added CZ pairs so every original CZ has the requested average repetition count across variants and per-block actual scale averages exactly to the requested scale. Preserve all R gates, classical mapping and ideal probabilities.
- [x] Freeze 10 batches of 100 circuits, each containing all five scales and four variants for five consecutive saved schedule blocks. Shuffle within each batch with a recorded seed. Archive counts of CZ, actual scales, per-gate repetitions, hashes and explicit DD strategy before any hardware submission.
- [x] Add scripts/iqm_bell_interleaved_analysis.py. Keep zero-weight leakage in the denominator. Calculate per-variant shot variance before aggregation; average variants, then blocks within settings, then sum nine settings. Original all-scale OLS linear estimator is primary. For quadratic fits use actual first/second scale moments per setting so averaging folded circuits is not silently treated as one exact circuit scale. Report global Bell linear lack-of-fit and separate per-setting quadratic lack-of-fit. Multinomial bootstrap repeats each variant; paired stratified block bootstrap is exploratory and does not cover shared job drift.
- [x] Test folding identities/scale balancing, interleaving/budget, corrupted input rejection, calibration checks, DD options, resume-without-resubmit and uncertain-submission refusal. Test model recovery on synthetic linear/quadratic responses with actual scale moments and unequal setting frequencies. Run targeted coverage >=80% where practical; independent reviewer inspects complete new implementation and tests before hardware.
- [x] Read IQM architecture, reuse physical layout from prior run when valid, pin calibration and validate serializable native circuits. Execute with explicit opt-in and file lock. Checkpoint job ID before waiting, retrieve known jobs after interruption, never resubmit unknown submissions. Stop on failed hardware job while preserving completed evidence.
- [x] Analyze all 250000 shots, compare prior three-scale benchmark without reusing old observations, generate JSON/Markdown/plot artifacts and completion audit. Validate replay, actual scales and hashes; report sampling/model limitations.

No other source files or previous artifact directories need modification. Subagent work is limited to independent review under the user's quality loop. The approved design is implemented locally; the previously read writing-plans/executing-plans workflows guide planning, validation and review, with user/developer authorization taking precedence over additional approval or commit steps.
